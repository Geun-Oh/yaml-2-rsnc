import { RenderWithJSON, RenderComponent } from './renderDst';
import { renderPage, renderComponent } from './dst';

const y2r = {
    RenderWithJSON,
    RenderComponent,
    renderPage,
    renderComponent,
}

export default y2r;